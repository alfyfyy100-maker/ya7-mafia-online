# فحص أمني شامل — ya7-mafia-online

مراجعة كل الألعاب في `worker.js` (١٧٬١٥٠ سطرًا، ١٨ صنف Durable Object)
بمنهج المخترِق الأخلاقي: **هجمات فعلية على وركر يعمل**، لا قراءة كود.
كل ثغرة أدناه أُثبتت بهجوم، ثم أُصلحت، ثم أُعيد الهجوم نفسه للتأكد.

الأداة: `workerd` الحقيقي عبر miniflare — كائنات حقيقية، تخزين حقيقي،
مقابس حقيقية.

---

## الخلاصة

| # | الثغرة | الخطورة | الحالة |
|---|---|---|---|
| ١ | **لوحة «الشفرة» السرّية تُولَّد بـ`Math.random`** | **عالية** | أُصلحت |
| ٢ | **لا سقف لحجم رسالة السوكِت الواردة** (١٦ غرفة) | متوسطة | أُصلحت |
| ٣ | **حقن `__proto__` في مفاتيح خريطة المطاردة** | متوسطة-منخفضة | أُصلحت |
| ٤ | **استثناء غير ملتقَط ⇒ 500 بلا CORS مع تسريب نصّ الخطأ** | متوسطة-منخفضة | أُصلحت |
| ٥ | قرارات حاسمة أخرى على `Math.random` (٨ مواضع) | منخفضة | أُصلحت |

---

## ١ — لوحة «الشفرة» كلها من `Math.random` 🔴

**الملف يحمل قاعدته بنفسه** (سطر ٦٩):

> «Math.random في V8 هو xorshift128+ ويُعكَس من أربع مخرجات متتالية.
> كل ما يقرّر دورًا أو كرتًا أو رمز غرفة يمر من هنا [randInt].»

القاعدة مطبَّقة في كل مكان **إلا الشفرة** — وهي بالضبط اللعبة التي
لوحتُها هي سرُّها كلّه:

```js
function shifraShuffle(a) { … const j = (Math.random() * (i + 1)) | 0; … }
pool  = shifraShuffle([...]).slice(0, 25);   // أي ٢٥ كلمة
roles = shifraShuffle([...9 أحمر, 8 أزرق, 7 محايد, "ass"]);  // ← المفتاح كلّه
g.first     = Math.random() < .5 ? "red" : "blue";
g.informers[t] = agents[(Math.random() * agents.length) | 0].id;   // هوية المخبر
g.leak[team]   = pool[(Math.random() * pool.length) | 0];          // الكلمة المسرَّبة
```

**سيناريو الهجوم**: اللوحة كاملةً — بما فيها القاتل — تُكشف للجميع في
نهاية كل جولة (`seesKey = ended || spymaster`). فمن أنهى جولةً واحدة
يملك مخرجات كافية لاستخراج حالة xorshift128+، ثم يتنبّأ بلوحة الجولة
التالية في نفس العزلة: يعرف القاتل والمفتاح كلَّه وهو **ليس قائد فريق**.
واللعبة كلها مبنية على أن هذا مستحيل.

**الإصلاح**: `randInt` (crypto) في المواضع الأربعة.

**التحقق**: ٦ لوحات متتالية — كلها مختلفة، ولا نمط ثابت في أي خانة.

---

## ٢ — لا سقف لحجم رسالة السوكِت الواردة 🟠

الخانق `MSG_PER_SEC = 12` **يعدّ الرسائل لا البايتات**. وسقف كلاودفلير
للرسالة الواحدة ميغابايت. فما كان بينهما شيء:

**١٢ ميغابايت في الثانية من `JSON.parse` على كائن واحد، من لاعبٍ واحد.**

الدردشة وحدها كان لها سقف (`raw.length > 4 * CHAT_LEN`). أما الغرف
الستّ عشرة فتحلّل ما يصلها كما جاء.

**الإثبات**: رسالة ١ ميغابايت قُبِلت وحُلِّلت؛ ثم اثنتا عشرة مثلها مرّت
في ٥٧٨ مللي ثانية بلا أي مقاومة.

**الإصلاح** — نقطة واحدة تغطي كل صنف حاضر وقادم:

```js
const WS_MSG_MAX = 16 * 1024;          // رسائل اللعب كلها نصوص قصيرة
function wsOversize(raw, max) { … }
```

طُبِّق في `applyRoomCommon` على **مسارَي الاستقبال معًا** — `onMessage`
(الغرف العادية) و`webSocketMessage` (غرف السبات: البلياردو، البلوت) —
وقبل `JSON.parse` لا بعده، لأن التحليل نفسه هو التكلفة. والأصناف الثلاثة
خارج `applyRoomCommon` (الشفرة، المطاردة، سباق المربعات) أُضيف لها الفحص
في مكانه.

طاريك وحدها ترفع سقفها (`WS_MAX = TARI_CLIP_MAX * 2 + 4096`) لأنها
تحمل صوتًا — والضِّعف مقصود، انظر «ما كسرتُه ثم أصلحتُه» أدناه.

---

## ٣ — حقن `__proto__` في خريطة المطاردة 🟠

اسم المكان يصير مفتاحًا في كائن الجوار:

```js
const adj = {};
for (const n of clean) adj[n] = [];        // n من cleanText، و__proto__ يمرّ منها
```

`obj['__proto__'] = x` **لا يُنشئ خاصية** — يبدّل النموذج الأولي. فالنتيجة
حالةٌ متناقضة: الاسم موجود في `districts` وغائب عن `Object.keys(adj)`.

**الإثبات** (هجوم فعلي، مضيفٌ يرسل الخريطة):

```
phase        : night          ← المباراة بدأت فعلًا
districts    : ["__proto__","حي أ", …]
adj keys     : ["حي أ", …]     ← __proto__ غائب
move opts    : [… ,"__proto__"] ← ومعروض وجهةً للانتقال
```

ومكانٌ وهميٌّ ليس في خريطة الجوار يعني منطق حركةٍ يقيس شيئًا ويحكم على
غيره. وأي دورة `JSON` على ذلك الكائن تحوّله إلى انهيار:

```
TypeError: round.__proto__.includes is not a function
```

**الإصلاح**: `safeKey()` مشتركة ترفض `__proto__`/`constructor`/`prototype`
لكل نصٍّ يصير مفتاحًا، مع `hasOwnProperty` بدل `!adj[s]` في فحص الجوار.

**التحقق**: خريطة فيها الثلاثة ⇒ تُشطب الثلاثة، وتبدأ المباراة بالستة
السليمة، و`districts` = `Object.keys(adj)` بالضبط.

---

## ٤ — استثناء غير ملتقَط ⇒ 500 بلا CORS مع تسريب نصّ الخطأ 🟠

`verifyToken` تنادي `hmac(env.ACCOUNT_SECRET, …)` بلا فحص. وحين ينقص
السرّ فإن `TextEncoder.encode(undefined)` تعطي مصفوفةً فارغة، و
`crypto.subtle.importKey` ترمي — ولا أحد يلتقطها في الراوتر.

**الإثبات**:

```
بلا ACCOUNT_SECRET  → 500  CORS=(بلا)  «DataError: Imported HMAC key length (0)…»
ACCOUNT_SECRET=""   → 500  CORS=(بلا)  «DataError: Imported HMAC key length (0)…»
```

ثلاثة أعطال في واحد: (أ) كل طلب يحمل `?acc=` يسقط، (ب) نصّ الخطأ الداخلي
يخرج للعميل، (ج) بلا CORS يرى اللاعب «Failed to fetch» بدل السبب — وهو
بالضبط ما يحذّر منه تعليق الملف نفسه في مواضع أخرى.

> **تصحيح فرضيةٍ بدأتُ بها**: ظننت أول الأمر أن نقص السرّ يعني توقيعًا
> بمفتاحٍ معلوم (`"undefined"`) ⇒ انتحال حسابات. جرّبتُها فلم تصحّ:
> `/account/*` تفشل مغلقةً بـ`binding-missing`، و`hmac` ترمي قبل ذلك.
> العطل حقيقي، لكنه **إتاحة لا انتحال**. أُثبتته كما هو، لا كما توقّعته.

**الإصلاح**: `verifyToken` ترجع `null` بلا سرّ (فشلٌ مغلق صامت)، وحزام
أمان حول الراوتر كله يرجع `server-error` عامًّا **بترويسات CORS دائمًا**.

---

## ٥ — قرارات حاسمة أخرى على `Math.random` 🟡

| الموضع | ما يقرّره |
|---|---|
| `pickRandom` | هدف البوت (من يُقتل، من يُنقَذ) |
| `wlPick` | مشهد وليمة، الميول، هدف اللاعب |
| مافيا: حسم التعادل | **من يموت** |
| لمن العرش: حسم التعادل | **من يُتَّهم** |
| مَوِّه: اختيار السؤال | السؤال **سرٌّ عن المموِّه** |
| فَطِن: السؤال والفئة والقرعة | معرفةُ السؤال قبل غيرك سبقٌ في السرعة |

كلها صارت `randInt`.

**ما بقي على `Math.random` عن قصد**: رميات البوتات الاحتمالية
(`Math.random() < 0.4`) — سياسة الملف المكتوبة، وما فيها سرٌّ يُتوقَّع،
إنما إيقاعُ سلوك. وأرقامُ تمييز الأسماء المكرّرة، واختيارُ جملة حوارٍ
للبوت، وعيّنةُ التنظيف (٢٪).

---

## ما فحصتُه فوجدتُه سليمًا

ليست كل النتائج ثغرات — وهذا جزء من التقرير:

| المحور | النتيجة |
|---|---|
| **حقن SQL** | ٨٦ استعلامًا، كلها `.bind()` — ولا قالب نصيّ واحد |
| **لوحة الإدارة** | مقارنة ثابتة الزمن + خانقان (عام وللمحاولات الفاشلة) + فشلٌ مغلق بلا الربطات |
| **صندوق SQL الإداري** | قراءة فقط مفروضة، جملة واحدة، PRAGMA ممنوعة، سقف ٢٠٠٠ حرف |
| **توكن الحساب** | HMAC-SHA256 + مقارنة ثابتة الزمن + `token_ver` للإبطال + فحص الحظر |
| **سرّية الأدوار** | كل لعبة تبني حالةً لكل مشاهد: الشفرة `seesKey`، المطاردة `over`، مافيا `yourRole` خاصة |
| **حقن نصّي** | `cleanName`/`cleanText` تشطبان محارف HTML والتحكّم **ومحارف قلب الاتجاه** |
| **انتحال المقعد** | الاستعادة بالاسم ممنوعة على مقعد المضيف وعلى أي مقعد مربوط بحساب |
| **الردهة العامة** | قائمة عمليات مغلقة + توكن مقعد المضيف + عدد اللاعبين من الخادم لا من العميل |
| **الدردشة** | تمرّ على `/seat-check`، بسقف طول وسقف مقابس وتنقية |
| **تلوّث النموذج الأولي عبر معرّفات اللاعبين** | `RESERVED_IDS` تسدّه |
| **تخمين رموز الغرف** | ٦٠ اتصالًا/دقيقة لكل بادئة IP؛ فضاء الرموز ~١٫١ مليار والغرف تنتهي بعد ٦ ساعات |
| **حجم أجسام POST** | حارس 413 قبل أي قراءة |
| **صلاحيات المضيف** | كل بدء/إعداد/طرد/إعادة في الألعاب الـ١٨ محروس |
| **أهداف الليل** | مافيا ولمن العرش تتحقّقان من وجود الهدف وحياته قبل التخزين |

---

## ما كسرتُه ثم أصلحتُه

سقفُ الرسالة أوّل ما كتبتُه كان `TARI_CLIP_MAX + 4096` — أي بقدر أكبر
مقطع بالضبط. فسقط اختبار كان ناجحًا: مقطعٌ زائد قليلًا صار يُبتلَع
بصمت قبل أن يصل فحص `onClip`، فيضيع تسجيل اللاعب **بلا رسالة تفسّر له
شيئًا**.

الإصلاح على الجهتين:
- الخادم: السقف **ضِعف** سقف المقطع — فالتجاوز المعقول (متصفّح يسجّل
  بمعدّل أعلى) يصل ويُردّ برسالة مفهومة، وما فوقه إساءةٌ تُهمَل بصمت.
- الصفحة: فحصٌ قبل الإرسال أصلًا، بالسقف كما يبثّه الخادم (`clipMax`)
  لا برقمٍ منسوخ في مكانين.

---

## ما لم أغيّره عمدًا

- **`gameNS` ينقصها `shifra` و`mutarada` و`squares`** — فدردشة الغرفة
  مغلقة في هذي الثلاث. أضفتُ `tari` وحدها لأن لعبتها تحتاجها. فتحُ
  الثلاث قرارُك: قد يكون إغلاقها مقصودًا.
- **`accountLive` تفشل مفتوحةً عند خطأ القاعدة** (`catch { ok = true }`) —
  فالمحظور يمرّ أثناء عطل D1. مقايضةُ إتاحةٍ مقصودة على ما يبدو؛ أُبلغ
  عنها ولا أقلبها من طرفي.
- **بذرةُ محرّك البلياردو الاحتياطية على `Math.random`** — المحرّك
  مستخرَجٌ آليًا من `bilyardo/index.html` ولازم يبقى مطابقًا حرفًا بحرف،
  وتعديله هنا يفرّق النسختين فيختلف حكمُ الخادم عن شاشة اللاعب. والنداءُ
  يمرّر بذرةً من `crypto` دائمًا، فالاحتياطي لا يُبلَغ عمليًا.

---

## التحقق النهائي

| | النتيجة |
|---|---|
| هجمات ما بعد الإصلاح | **١٦ / ١٦** |
| اختبارات اللعبة (انحدار) | **٢٦٩ / ٢٦٩** |
